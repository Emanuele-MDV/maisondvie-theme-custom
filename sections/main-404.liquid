<style type="text/css">
  .template-404 .title + * {
    margin-top: 1rem;
  }

  @media screen and (min-width: 750px) {
    .template-404 .title + * {
      margin-top: 2rem;
    }
  }
</style>

<div class="template-404 page-width page-margin center">
  <p>
    {{ 'templates.404.subtext' | t }}
  </p>
  <h1 class="title">
    {% if section.settings.custom_text == blank %}{{ 'templates.404.title' | t }}{% else %}{{ section.settings.custom_text }}{% endif %}
  </h1>
  <a href="{% if section.settings.custom_button != blank %}{{ section.settings.custom_button }}{% else %}{{ routes.all_products_collection_url }}{% endif %}" class="button">
    {{ 'general.continue_shopping' | t }}
  </a>
</div>

{% schema %}
{
  "name": "404 page settings",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "custom_text",
	  "label":"Customize the 404 message"
    },
	{
		"type":"url",
		"id":"custom_button",
		"label":"Change the button's link"
	},
	{
		"type":"text",
		"id":"custom_css_class",
		"label":"Add custom class?"
	}
  ]
}
{% endschema %}
